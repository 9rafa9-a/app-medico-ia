name: Build Flet App

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          path: mobile

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python Dependencies
        working-directory: mobile
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          # cache: true (DISABLED)

      - name: Verify Flutter Version
        run: flutter --version

      - name: Accept Android Licenses
        run: |
          yes | flutter doctor --android-licenses || true

      - name: Install Android NDK
        run: |
          echo "y" | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "ndk;26.1.10909125"

      - name: Build APK (Force Native Libs & Main Script)
        working-directory: mobile
        run: |
          echo "=== 1. LIMPEZA E PREPARAÇÃO ==="
          # Limpa builds anteriores para evitar fantasmas
          rm -rf build
          
          # Gera a estrutura Android base (scaffolding)
          flet build apk --verbose --clear-cache || true
          
          echo "=== 2. LOCALIZANDO PASTAS CRÍTICAS ==="
          SITE_PACKAGES_PATH=$(find $(pwd) -type d -name "site-packages" | head -n 1)
          ANDROID_ROOT=$(find $(pwd) -name "gradlew" -type f | xargs dirname | head -n 1)
          
          if [ -z "$ANDROID_ROOT" ]; then
            echo "❌ ERRO: Projeto Android não gerado."
            exit 1
          fi
          
          echo "✅ Android Root: $ANDROID_ROOT"
          
          # === A CORREÇÃO DE OURO: FORÇAR O MAIN.PY ===
          # O Flet espera o código em: [pasta_android]/app/src/main/assets/app/main.py
          ASSETS_APP_DIR="$ANDROID_ROOT/app/src/main/assets/app"
          
          echo "=== 3. CIRURGIA NO MAIN.PY ==="
          # Garante que a pasta existe
          mkdir -p "$ASSETS_APP_DIR"
          
          # Verifica se o seu main.py existe na pasta atual (mobile)
          if [ -f "main.py" ]; then
             echo "✅ main.py encontrado na raiz 'mobile'. Copiando..."
             cp "main.py" "$ASSETS_APP_DIR/main.py"
             echo "🚀 SUCESSO: main.py injetado manualmente nos assets!"
          else
             echo "❌ ERRO CRÍTICO: main.py não encontrado na pasta $(pwd)"
             ls -la
             exit 1
          fi
          
          # Lista para confirmar
          echo "Conteúdo final da pasta assets/app:"
          ls -la "$ASSETS_APP_DIR"
          
          echo "=== 4. COMPILAÇÃO GRADLE ==="
          cd "$ANDROID_ROOT"
          chmod +x gradlew
          export SERIOUS_PYTHON_SITE_PACKAGES="$SITE_PACKAGES_PATH"
          
          # Compila
          ./gradlew clean assembleRelease --stacktrace
          
          echo "=== 5. RESGATE DO APK ==="
          cd "$GITHUB_WORKSPACE/mobile"
          BUILT_APK=$(find . -name "*.apk" | grep "release" | head -n 1)
          
          if [ -z "$BUILT_APK" ]; then
             echo "❌ ERRO: APK não gerado."
             exit 1
          fi
          
          mkdir -p build/apk
          cp "$BUILT_APK" build/apk/app-release.apk
          echo "✅ APK PRONTO PARA DOWNLOAD"

      # ADICIONE ESTE BLOCO AQUI PARA DIAGNÓSTICO
      - name: Inspect APK Content (Debug)
        working-directory: mobile
        run: |
          sudo apt-get install unzip
          echo "==== CONTEÚDO DA PASTA LIB/ARM64-V8A ===="
          unzip -l build/apk/app-release.apk | grep "lib/arm64-v8a/"
          echo "========================================="

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: mobile/build/apk/app-release.apk

  build-windows:
    name: Build Windows EXE
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          path: mobile

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Python Dependencies
        working-directory: mobile
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          # cache: true (DISABLED)

      - name: Build Windows EXE
        working-directory: mobile
        run: |
          flet build windows --verbose

      # Flet/Flutter builds usually output to build/windows/runner/Release/
      # We upload the whole folder or just the exe and necessary DLLs if needed.
      # For Flet localized builds, it might be simpler to just zip the Release folder.
      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-exe
          path: mobile/build/windows
